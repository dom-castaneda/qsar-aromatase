---
created: 2026-06-30T04:08:32.109Z
session: 06b31b5a-56da-46e6-bf29-3f66231020ee
working_directory: C:\Users\dommy\0_self_projects\qsar_aromatase
---

## Context

- **Target**: pchembl_value (regression)
- **Split**: Random 80/20 (seed=42)
- **Fingerprint**: MACCS filtered (114 bits)
- **Models**: 16 regression algorithms across all major families

## All 16 Models

| # | Algorithm | Key Params |
|---|-----------|-----------|
| 1 | Ridge | alpha=1.0 |
| 2 | Lasso | alpha=0.1 |
| 3 | ElasticNet | alpha=0.1, l1_ratio=0.5 |
| 4 | Bayesian Ridge | defaults |
| 5 | PLS Regression | n_components=10 |
| 6 | KNN Regressor | n_neighbors=5 |
| 7 | SVR (RBF) | C=1.0, epsilon=0.1 |
| 8 | Kernel Ridge (RBF) | alpha=1.0, kernel='rbf' |
| 9 | Decision Tree | random_state=42 |
| 10 | Random Forest | n_estimators=500, random_state=42 |
| 11 | Extra Trees | n_estimators=500, random_state=42 |
| 12 | Gradient Boosting | n_estimators=500, random_state=42 |
| 13 | XGBoost | n_estimators=500, learning_rate=0.1, random_state=42 |
| 14 | Hist Gradient Boosting | max_iter=500, random_state=42 |
| 15 | AdaBoost | n_estimators=500, random_state=42 |
| 16 | MLP Regressor | hidden_layers=(256,128), max_iter=500, random_state=42 |

## Performance Metrics (reported for both train and test)
- **R²** (coefficient of determination)
- **RMSE** (root mean squared error)

## Implementation Steps

### Step 1: Install xgboost, create `requirements.txt`

### Step 2: Create `scripts/08_build_models.py`
1. Load train/test molecule IDs from `data/splits/`
2. Load full dataset + filtered MACCS FPs, align by row mask
3. Build X_train, X_test, y_train, y_test (seed=42 for the split already done)
4. Train all 16 models
5. Predict on train and test
6. Compute R² and RMSE for train and test
7. Print ranked results table (sorted by test R²)
8. Save `data/models/model_results_maccs.csv` (all metrics)
9. Save `data/models/predictions_maccs.csv` (test set predictions from all models)

### Step 3: Verification
- All 16 models complete without error
- Results table sorted by test R²
- Confirm no data leakage (train R² >= test R²)

## Critical Files
- `scripts/08_build_models.py` — New ML training script
- `requirements.txt` — Project dependencies with latest versions
- `data/splits/random_train.csv` — Train molecule IDs + pchembl
- `data/fingerprints_filtered/fingerprints_maccs.csv` — MACCS features (114 bits)