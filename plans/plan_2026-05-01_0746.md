---
created: 2026-05-01T07:46:31.757Z
session: 06b31b5a-56da-46e6-bf29-3f66231020ee
working_directory: C:\Users\dommy\0_self_projects\qsar_aromatase
---

## Context

**Dataset**: `aromatase_bioactivity_clean.csv` — 3,774 records, 3,399 unique molecules, 19 columns.

Key findings from exploration:
- **pchembl_value**: 3,296 non-null (478 missing), range 4.0–10.82, mean 6.37, good spread for QSAR
- **standard_type**: IC50 (3,211), Ki (516), pIC50 (47)
- **standard_relation**: `=` (3,403), `>` (275), `>=` (1) — 95 missing
- **standard_units**: nM (3,632), uM (30), ug/mL (28), 84 missing — unit heterogeneity to flag
- **assay_type**: B (binding, 3,707), A (ADME, 67)
- **document_year**: 1978–2024 — temporal span for trend analysis
- 327 unique assays, 9 journals, 154 named molecules
- All viz libraries available: matplotlib, seaborn, RDKit (Descriptors, Draw), sklearn (PCA, tSNE), scipy

**Deliverable**: A Jupyter notebook `notebooks/eda_aromatase.ipynb` with reproducible, publication-quality EDA.

---

## Implementation Steps

### 1. Setup & Data Loading
- Imports (pandas, numpy, matplotlib, seaborn, RDKit, sklearn)
- Load `aromatase_bioactivity_clean.csv`
- Overview table: shape, dtypes, null counts, unique counts

### 2. Bioactivity Distribution Analysis
- **pchembl_value histogram** with KDE overlay, colored by standard_type (IC50/Ki/pIC50)
- **Box plot** of pchembl_value by standard_type
- **Activity class definition**: define active/inactive/intermediate thresholds (pchembl >= 6.5 active, < 5.0 inactive, between = intermediate) — standard QSAR convention
- **Pie/bar chart** of activity class balance

### 3. Data Quality & Completeness
- **Heatmap of missing values** per column
- **standard_relation breakdown**: exact (`=`) vs censored (`>`, `>=`) — flag censored data
- **Unit heterogeneity**: bar chart of standard_units, flag non-nM entries
- **Duplicate molecule analysis**: how many molecules appear in multiple assay types

### 4. Temporal Trends
- **Publications over time**: bar chart of document_year
- **pchembl_value vs year**: scatter/violin showing whether potency has improved over decades
- **Cumulative molecule discovery curve**

### 5. Molecular Property Distributions (Lipinski / Drug-likeness)
- Compute from SMILES via RDKit: MW, LogP, HBA, HBD, TPSA, rotatable bonds, aromatic rings, fraction Csp3
- **Histograms** of each property
- **Lipinski Rule-of-5 compliance** bar chart
- **Scatter matrix** of key properties (MW vs LogP, colored by activity class)
- **MW vs pchembl_value** and **LogP vs pchembl_value** scatter plots

### 6. Chemical Space Visualization
- Load the ECFP4 fingerprints (1,024 bits — best for chemical space)
- **PCA** (2D) colored by pchembl_value (continuous) and activity class (categorical)
- **t-SNE** (2D) colored by activity class
- Variance explained bar chart from PCA

### 7. Fingerprint Bit Frequency Analysis
- For each FP type (MACCS 167, SubFP 307, KRFP 4860): compute bit frequency across all molecules
- **MACCS key frequency bar chart** — which structural features are most/least common
- **Correlation of top MACCS bits with pchembl_value** (point-biserial) — which substructures associate with activity
- **KRFP sparsity analysis** — histogram of bits-per-molecule, fraction of always-zero bits

### 8. Correlation & Feature Importance Preview
- **Correlation heatmap** of molecular properties vs pchembl_value
- **Top 20 most correlated fingerprint bits** (across all FP types) with pchembl_value
- Brief **inter-fingerprint redundancy check** (correlation between FP types via Tanimoto similarity)

### 9. Summary & QSAR Readiness Assessment
- Markdown summary cell: dataset size, class balance, chemical diversity, data quality flags
- Recommendations for modeling: which activity class split, which FP types, censored data handling

---

## Verification
- Run all cells top-to-bottom (Execute All) and confirm no errors
- Verify all plots render (check cell outputs for images)
- Confirm activity class counts sum to total records with pchembl_value
- Cross-check molecular property stats against known drug-like ranges

## Critical Files
- `data/processed/aromatase_bioactivity_clean.csv` — Primary input (3,774 rows, 19 columns)
- `data/fingerprints/fingerprints_ecfp4.csv` — ECFP4 FPs for chemical space viz
- `data/fingerprints/fingerprints_maccs.csv` — MACCS keys for interpretable bit analysis
- `data/fingerprints/fingerprints_kr.csv` — KR FP for sparsity/feature analysis
- `notebooks/eda_aromatase.ipynb` — New notebook to create